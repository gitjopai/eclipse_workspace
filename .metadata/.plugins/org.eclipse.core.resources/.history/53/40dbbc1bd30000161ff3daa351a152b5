public class Textanalyse {
	public static void main (String []args){
		String zuanalysierenderSatz = "4Das ! ist doch schön oder 4";
		int zaehler;
		int Konsonanten = 0;
		for (zaehler = 0; zaehler < zuanalysierenderSatz.length(); zaehler++){
			if (zuanalysierenderSatz.charAt(zaehler) == ('b' || 'c' || 'd'|| 'f' || 'g' || 'h' || 'j' || 'k' || 'l' || 'm' || 'n' || 'p' || 'q' || 'r' || 's' || 't' || 'v' || 'w' || 'x' || 'y' || 'z'){
				Konsonanten = Konsonanten +1;
			}
		}
		System.out.println(+Konsonanten);
	}
}